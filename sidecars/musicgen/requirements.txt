# Ranged pins on purpose: there is no verified local musicgen image to derive
# exact versions from (the MusicGen studio is optional, -IncludeMusicGen).
# Pin exactly from `pip freeze` of a working image before relying on it.
audiocraft>=1.3
# audiocraft 1.3 pins torch 2.1; newer transformers/hub releases refuse it.
transformers>=4.31,<4.40
huggingface_hub<1.0
fastapi>=0.115
uvicorn[standard]>=0.30
soundfile>=0.12
numpy<2
