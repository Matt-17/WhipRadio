# torch is installed in the Dockerfile (CPU or CUDA via TORCH_INDEX build arg)
# and is deliberately NOT pinned here.
# Exact pins taken from the verified whipradio-tts:local image (2026-07-03) so
# rebuilds are reproducible. transformers 5.x refuses the cu121 torch (2.5.x) —
# Kokoro's AlbertModel fails to import — so stay on 4.x when bumping.
transformers==4.57.3
fastapi==0.137.2
uvicorn[standard]==0.49.0
kokoro==0.9.4
piper-tts==1.4.2
qwen-tts==0.1.1
huggingface_hub==0.36.2
soundfile==0.14.0
numpy==2.4.6
